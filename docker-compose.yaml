services:
  updaemon-db:
    image: mysql:5.7-debian
    platform: linux/amd64
    user: "1000:50"
    volumes:
      - updaemon-data:/var/lib/mysql
    environment:
      - MYSQL_RANDOM_ROOT_PASSWORD=true

  run-migrations:
    image: debian:stable
    command: "./wait-for updaemon-db:3306 -- /opt/venv/bin/python /opt/updaemon/db/run_migrations.py --config updaemon.conf --dir /opt/updaemon/db/migrations"
    depends_on:
      - updaemon-db
    volumes:
      - ./:/opt/updaemon

  updaemon:
    build: .
    command: "cron && tail -f /var/log/cron.log"
    image: debian:stable
    depends_on:
      - updaemon-db
      - run-migrations
    tmpfs:
      - /tmp
    volumes:
      - ./:/opt/updaemon
      
volumes:
  updaemon-data: {}